services:
  mydictionaryapi:
    image: ghcr.io/davien7115/dictionary-backend:latest
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ConnectionStrings__DefaultConnection=Host=dictionary_db;Database=library_db;Username=lib_user;Password=libu123
    ports:
      - "7260:8080"
    depends_on:
      dictionary_db:
       condition: service_healthy

  mydictionaryapp:
    image: ghcr.io/davien7115/dictionary-frontend:latest
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8080
    ports:
      - "7195:8080" 
    depends_on:
      - mydictionaryapi

  dictionary_db:
    image: postgres:latest
    container_name: dictionary_db
    environment:
      POSTGRES_USER: lib_user
      POSTGRES_PASSWORD: libu123
      POSTGRES_DB: library_db
    volumes:
      - postgres_data:/var/lib/postgresql/
    ports:
      - "5432:5432"
    healthcheck:
          test: ["CMD-SHELL", "pg_isready -U lib_user -d library_db"]
          interval: 5s
          timeout: 5s
          retries: 5

volumes:
  postgres_data: